# Copy this file to .env and fill in real values. Never commit .env.
#
# On Hostinger's Node.js App hosting, don't upload a .env file at all --
# set these as Environment Variables in hPanel's Node.js app screen instead.
NODE_ENV=development
PORT=4000

DB_HOST=srv1427.hstgr.io
DB_PORT=3306
DB_NAME=u785833190_awx_db
DB_USER=u785833190_awx_user
DB_PASSWORD=changeme

JWT_ACCESS_SECRET=changeme_access_secret
JWT_REFRESH_SECRET=changeme_refresh_secret
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=30d

# Comma-separated list of origins allowed to call this API.
# Local dev: http://localhost:5173,http://localhost:5174
# Production (awxchange.com): https://awxchange.com,https://www.awxchange.com,https://admin.awxchange.com
CORS_ORIGINS=http://localhost:5173,http://localhost:5174

SEED_ADMIN_EMAIL=admin@assetwave.com
SEED_ADMIN_PASSWORD=changeme_admin_password
